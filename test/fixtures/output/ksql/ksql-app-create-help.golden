Create a ksqlDB app.

Usage:
  ccloud ksql app create <name> [flags]

Flags:
      --csu int32            Number of CSUs to use in the cluster. (default 4)
  -o, --output string        Specify the output format as "human", "json", or "yaml". (default "human")
      --api-key string       Kafka API key for the ksqlDB cluster to use (recommended).
      --api-secret string    Secret for the Kafka API key (recommended).
      --environment string   Environment ID.
      --cluster string       Kafka cluster ID.
      --context string       CLI Context name.

Global Flags:
  -h, --help            Show help for this command.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Did you know you can use the `ccloud feedback` command to send the team feedback?
Let us know if the CLI is meeting your needs, or what we can do to improve it.
