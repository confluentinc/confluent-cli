Manage Schema Registry cluster.

Usage:
  ccloud schema-registry cluster [command]

Available Commands:
  describe    Describe the Schema Registry cluster for this environment.
  enable      Enable Schema Registry for this environment.
  update      Update global mode or compatibility of Schema Registry.

Global Flags:
  -h, --help            Show help for this command.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "ccloud schema-registry cluster [command] --help" for more information about a command.

Did you know you can use the `ccloud feedback` command to send the team feedback?
Let us know if the CLI is meeting your needs, or what we can do to improve it.
